https://www.codechef.com/problems/BREAKSTICK?tab=statement
#include <stdio.h>

int main(void)
{
    int t,n,x;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d%d",&n,&x);
        if(n%2==0||(n%2!=0 && x%2!=0))    printf("yes\n");
        else    printf("no\n");
    }

	return 0;
}
