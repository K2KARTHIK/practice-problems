https://www.codechef.com/problems/VALIDMIN?tab=statement
#include <stdio.h>

int main() 
{
    int t;
    scanf("%d", &t);

    while (t--) {
        int x, y, z;
        scanf("%d %d %d", &x, &y, &z);

        if ((x == y && x <= z) || (y == z && z <= x) || (z == x && z <= y))
            printf("YES\n");
        else
            printf("NO\n");
    }
    return 0;
}
